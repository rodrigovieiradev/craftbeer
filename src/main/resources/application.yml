server:
  port: 9000
  context-path: /beerhouse
  
spring:
  datasource:
    driverClassName: org.h2.Driver
    url: 'jdbc:h2:mem:beerhousedb'
    username: sa
    password: ZUVn6ZYw$iVl
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true